query slides($top: Int, $skip: Int) {
  querySlideContents(top: $top, skip: $skip) {
    id
    data {
      linkUrl {
        iv
      }
      image {
        iv {
          url
        }
      }
    }
  }
}
